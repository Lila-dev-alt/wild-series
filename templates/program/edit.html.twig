{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block title %}Éditer un programme{% endblock %}

{% block body %}
    <h1 class="bg-warning text-center mt-3">Éditer un Programme</h1>
<div class="container m-auto d-flex justify-content-center w-80">
    {{ include('program/_form.html.twig') }}
</div>
<div class="d-flex justify-content-end">
    <a class="btn bg-primary mr-2" href="{{ path('program_index') }}">Retour à la liste</a>

    {{ include('program/_delete_form.html.twig') }}
</div>
{% endblock %}
