<nav class="navbar navbar-expand-lg  navbar-dark bg-primary">
    <a class="navbar-brand" href="/">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link font-weight-bold" href="/wild">Wild</a>
            <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('program_index') }}">Séries</a>
            <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('season_index') }}">Saisons</a>
            <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('episode_index') }}">Episodes</a>
            <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('actor_index') }}">Acteurs</a>
        </div>
    </div>
    {% if app.user %}


        <a class=" nav-link text-white font-weight-bold" href="/my-profile"> Mon compte</a>
        <a class=" nav-link text-white font-weight-bold" href="{{ path('app_logout') }}"> Se déconnecter</a>

    {% else %}
        <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('app_login') }}"> Se connecter</a>
        <a class="nav-item nav-link text-white font-weight-bold" href="{{ path('app_register') }}"> S'inscrire</a>
    {% endif %}
</nav>
