{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <h1 class="bg-warning text-center mt-3">Editer un Episode</h1>
<div class="container">
    {{ include('episode/_form.html.twig') }}
</div>
    <div class="container d-flex justify-content-center">
        <a class="text-white btn bg-primary mr-2" href="{{ path('episode_index') }}">Retour à la liste</a>

    {{ include('episode/_delete_form.html.twig') }}
    </div>
{% endblock %}
